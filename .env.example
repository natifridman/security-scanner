# GitHub API token with permissions to access repositories
# Needs: repo, read:org permissions
GITHUB_TOKEN=your_github_token_here

# Server configuration
PORT=50051

# Policy folder (optional, will use built-in policies if not specified)
POLICY_FOLDER=policies 